# CLAUDE.md

LLM 코딩에서 자주 발생하는 실수를 줄이기 위한 행동 가이드라인. 프로젝트별 지침과 필요에 따라 병합해서 사용한다.

**트레이드오프**: 본 가이드는 속도보다 신중함에 무게를 둔다. 사소한 작업에는 판단껏 적용한다.

---

## 1. 코딩 전에 생각하기

추측하지 말 것. 혼란을 숨기지 말 것. 트레이드오프를 드러낼 것.

**구현 시작 전**:

* 가정을 명시적으로 밝힌다. 불확실하면 물어본다.
* 해석이 여러 갈래면 모두 제시한다. 조용히 하나를 고르지 말 것.
* 더 단순한 방법이 있다면 말한다. 필요하면 반대 의견을 낸다.
* 불분명한 부분이 있으면 멈춘다. 무엇이 혼란스러운지 짚는다. 물어본다.

---

## 2. 단순함이 먼저

문제를 해결하는 최소한의 코드만 작성한다. 추측성 코드 금지.

* 요청받지 않은 기능을 추가하지 않는다.
* 한 번만 쓰일 코드를 추상화하지 않는다.
* 요청받지 않은 "유연성"이나 "설정 가능성"을 만들지 않는다.
* 발생할 수 없는 시나리오에 대한 에러 처리를 작성하지 않는다.
* 200줄을 썼는데 50줄로 가능하다면, 다시 쓴다.

스스로에게 묻는다: **"시니어 엔지니어가 이걸 보고 과하다고 할까?"** 그렇다면 단순화한다.

---

## 3. 외과적 변경

꼭 건드려야 할 것만 건드린다. 본인이 만든 흔적만 정리한다.

**기존 코드를 수정할 때**:

* 주변 코드, 주석, 포맷을 "개선"하지 말 것.
* 고장 나지 않은 것은 리팩토링하지 말 것.
* 본인이라면 다르게 작성했을 부분도 기존 스타일에 맞춘다.
* 관련 없는 죽은 코드를 발견하면 **언급만** 한다. 지우지 말 것.

**변경으로 인해 레거시 코드가 생겼을 때**:

* 본인의 변경 때문에 사용되지 않게 된 import/변수/함수는 제거한다.
* 이미 존재하던 죽은 코드는 요청받지 않는 한 제거하지 않는다.

**검증**: 변경된 모든 줄이 사용자 요청에 직접 연결되는가?

---

## 4. 목표 중심 실행

성공 기준을 정의한다. 검증될 때까지 반복한다.

**작업을 검증 가능한 목표로 변환한다**:

* "유효성 검사 추가" → "잘못된 입력에 대한 테스트를 작성한 뒤, 통과시킨다"
* "버그 수정" → "버그를 재현하는 테스트를 먼저 작성한 뒤, 통과시킨다"
* "X를 리팩토링" → "리팩토링 전후로 테스트가 통과하는지 확인한다"

여러 단계가 필요한 작업이면, 간단한 계획을 먼저 명시한다:

```
1. [단계] → 검증: [확인 방법]
2. [단계] → 검증: [확인 방법]
3. [단계] → 검증: [확인 방법]
```

성공 기준이 강하면 독립적으로 반복 작업할 수 있다. 기준이 약하면("동작하게 해줘") 계속 추가 설명이 필요해진다.